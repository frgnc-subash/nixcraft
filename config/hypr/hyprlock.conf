# ┬ ┬┬ ┬┌─┐┬─┐┬  ┌─┐┌─┐┬┌─
# ├─┤└┬┘├─┘├┬┘│  │ ││  ├┴┐
# ┴ ┴ ┴ ┴  ┴└─┴─┘└─┘└─┘┴ ┴

# -------------------
# Color Variables
# -------------------
source = $HOME/.config/hypr/theme.conf

# -------------------
# General Settings
# -------------------
general {
    ignore_empty_input = true
    hide_cursor = false
    no_fade_in = false
    grace = 0
}

# -------------------
# Background Settings
# -------------------
background {
    monitor =
    path = ~/Pictures/wallpapers/monochrome/wallpaper_001.png
    transparency = 0.5
    color = $surface
    blur_passes = 1
    noise = 0.012
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

animations {
    animation = fade,    1, 6, default
    animation = fadeIn,  1, 6, default
    animation = fadeOut, 1, 6, default
}
# -------------------
# User
# ------------------- 
label {
    monitor =
    text = <span>  </span>
    color = rgba(216, 222, 233, 0.70)
    font_size = 24
    font_family = DepartureMonoNerdFontMono
    position = 0, -40
    halign = center
    valign = top
}

image {
    monitor =
    path = /home/axosis/Pictures/misc/pfp.png
    size = 100
    border_color = $outline
    rounding = -1      
    position = 0, 10
    halign = center
    valign = center
}

label {
    monitor =
    text =  <span>hi,$USER</span>
    color = $primary
    font_size = 14
    font_family = DepartureMonoNerdFontMono
    position = 0, -65
    halign = center
    valign = center
}

# -------------------
# Input Field
# -------------------
input-field {
    monitor =
    size = 140, 28
    outline_thickness = 0
    dots_size = 0.3
    dots_spacing = 0.2
    dots_center = true
    dots_rounding = -1
    
    outer_color = $primary
    inner_color = $surface_container
    font_color = $on_surface
    
    fade_on_empty = false
    fade_timeout = 1000
    rounding = 60
    placeholder_text = <i>Input Password...</i>
    hide_input = false
    
    position = 0, -100
    halign = center
    valign = center
    
    fail_color = $error
    fail_text = <i>$ATTEMPTS attempts failed</i>
    fail_transition = 300
}

label {
    monitor =
    text = $TIME
    color = $primary
    font_size = 80
    font_family = DepartureMonoNerdFontMono Bold
    position = 0, 230
    halign = center
    valign = center
}
label {
    monitor =
    text = cmd[update:1000] sh -c 'date +%S'
    color = $error 
    font_size = 12
    font_family = DepartureMonoNerdFontMono Bold
    position = 180, 266    
    halign = center
    valign = center
}

# -------------------
# Date
# -------------------
label {
    monitor =
    text = cmd[update:1000] sh -c 'date "+%a, %b %d"'
    color = $on_surface_variant
    font_size = 12
    font_family = DepartureMonoNerdFontMono 
    position = 0, 140
    halign = center
    valign = center
}


# music player


# ============================================================================
# Music Player Widget
# ============================================================================

# ----------------------------------------------------------------------------
# Album Art
# ----------------------------------------------------------------------------
image {
    monitor =
    path = $HOME/.config/hypr/scripts/music.webp
    size = 100
    rounding = 5
    border_size = 3
    border_color = rgba(216, 222, 233, 0.70)
    rotate = 0
    reload_time = 2
    reload_cmd = ~/.config/hypr/scripts/hyprlock-music.sh --art
    position = -180,-275
    halign = center
    valign = center
    zindex = 1
}

# ----------------------------------------------------------------------------
# Player Background Box
# ----------------------------------------------------------------------------
shape {
    monitor =
    color = rgba(100, 114, 125, 0.4)
    size = 500, 140
    rounding = 16
    position = 0, 40
    halign = center
    valign = bottom
    zindex = 0
}

# ----------------------------------------------------------------------------
# Track Information
# ----------------------------------------------------------------------------

# Song title
label {
    monitor =
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --title)"
    color = rgba(216, 222, 233, 0.70)
    font_size = 14
    font_family = DepartureMonoNerdFontMono
    position = 55, 138
    halign = center
    valign = bottom
    zindex = 1
}

# Player source (e.g., Spotify, Firefox)
label {
    monitor =
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --player)"
    color = rgba(216, 222, 233, 0.70)
    font_size = 10
    font_family = DepartureMonoNerdFontMono
    position = 195, 45
    halign = center
    valign = bottom
    zindex = 1
}

# Artist name
label {
    monitor =
    text = cmd[update:1000] echo "󰠃  $(~/.config/hypr/scripts/hyprlock-music.sh --artist)"
    color = rgba(216, 222, 233, 0.70)
    font_size = 12
    font_family = DepartureMonoNerdFontMono
    position = 55, 118
    halign = center
    valign = bottom
    zindex = 1
}

# ----------------------------------------------------------------------------
# Playback Controls
# ----------------------------------------------------------------------------

# Previous track button
label {
    monitor =
    text = 󰒮
    color = rgba(216, 222, 233, 0.70)
    font_size = 25
    onclick = playerctl previous
    position = -5, 55
    halign = center
    valign = bottom
}

# Play/Pause button
label {
    monitor =
    text = cmd[update:1000] ~/.config/hypr/scripts/hyprlock-music.sh --status
    color = rgba(216, 222, 233, 0.70)
    font_size = 25
    onclick = playerctl play-pause
    position = 55, 55
    halign = center
    valign = bottom
}

# Next track button
label {
    monitor =
    text = 󰒭
    color = rgba(216, 222, 233, 0.70)
    font_size = 25
    onclick = playerctl next
    position = 110, 55
    halign = center
    valign = bottom
}

# ----------------------------------------------------------------------------
# Progress Display
# ----------------------------------------------------------------------------

# Current position timestamp
label {
    monitor = 
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --position)"
    color = rgba(216, 222, 233, 0.70)
    font_size = 10
    font_family = DepartureMonoNerdFontMono
    position = -100, 95
    halign = center
    valign = bottom
}

# Total length timestamp
label {
    monitor = 
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/hyprlock-music.sh --length)"
    color = rgba(216, 222, 233, 0.70)
    font_size = 10
    font_family = DepartureMonoNerdFontMono
    position = 210, 95
    halign = center
    valign = bottom
}

# Progress bar (with Pango markup for colored segments)
label {
    monitor = 
    text = cmd[update:1000] ~/.config/hypr/scripts/hyprlock-music.sh --progress-bar
    color = rgba(216, 222, 233, 0.70)
    font_size = 12
    font_family = DepartureMonoNerdFontMono
    position = 55, 95
    halign = center
    valign = bottom
}


